{"id":"go_rename_001","language":"go","agent":"RenameAgent","before":"func Calc(lst []Item, n int) int { x := 0; for i := 0; i < n; i++ { x += lst[i].Count }; return x }","after":"func Calc(items []Item, itemCount int) int {\n\ttotal := 0\n\tfor index := 0; index < itemCount; index++ {\n\t\ttotal += items[index].Count\n\t}\n\treturn total\n}","why_safe":"Only parameter and local variable names changed; logic and return value identical."}
{"id":"go_rename_002","language":"go","agent":"RenameAgent","before":"func chk(u *User) bool { return u != nil && u.Active && !u.Blocked }","after":"func canUserProceed(user *User) bool {\n\treturn user != nil && user.Active && !user.Blocked\n}","why_safe":"Unexported function and parameter renamed for clarity; boolean expression unchanged."}
