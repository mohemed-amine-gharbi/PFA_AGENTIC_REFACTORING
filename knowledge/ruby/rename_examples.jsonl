{"id":"ruby_rename_001","language":"ruby","agent":"RenameAgent","before":"def calc(lst)\n  x = 0\n  lst.each { |i| x += i.count }\n  x\nend","after":"def calc(items)\n  total = 0\n  items.each { |item| total += item.count }\n  total\nend","why_safe":"Only parameter and local variable names changed; logic and return value identical."}
{"id":"ruby_rename_002","language":"ruby","agent":"RenameAgent","before":"def chk(u)\n  u && u.active? && !u.blocked?\nend","after":"def can_proceed?(user)\n  user && user.active? && !user.blocked?\nend","why_safe":"Private method and parameter renamed for clarity; boolean expression unchanged."}
