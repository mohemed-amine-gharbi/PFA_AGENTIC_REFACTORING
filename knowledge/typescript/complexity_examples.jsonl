{"id":"ts_complexity_001","language":"typescript","agent":"ComplexityAgent","before":"function getTotal(order: Order | null): number { if (order !== null) { if (order.isActive) { if (order.items && order.items.length > 0) { return order.items.reduce((sum, i) => sum + i.price, 0); } else { return 0; } } else { return 0; } } return 0; }","after":"function getTotal(order: Order | null): number {\n  if (!order) return 0;\n  if (!order.isActive) return 0;\n  if (!order.items || order.items.length === 0) return 0;\n  return order.items.reduce((sum, i) => sum + i.price, 0);\n}","why_safe":"Same null/empty checks and return values; nesting reduced with guard clauses only."}
{"id":"ts_complexity_002","language":"typescript","agent":"ComplexityAgent","before":"function classify(score: number): string { if (score >= 90) { return 'A'; } else { if (score >= 80) { return 'B'; } else { if (score >= 70) { return 'C'; } else { return 'F'; } } } }","after":"function classify(score: number): string {\n  if (score >= 90) return 'A';\n  if (score >= 80) return 'B';\n  if (score >= 70) return 'C';\n  return 'F';\n}","why_safe":"All conditions and return values preserved; else-if chain flattened with early returns."}
